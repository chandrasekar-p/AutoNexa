# Copy to .env.local and adjust if your API isn't on the default port.
# Must be reachable from the browser (this is a NEXT_PUBLIC_ var, baked
# into client bundles) — same-origin or CORS-configured on the API side.
NEXT_PUBLIC_API_URL=http://localhost:4000
